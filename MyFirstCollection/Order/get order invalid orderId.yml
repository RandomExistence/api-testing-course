info:
  name: get order invalid orderId
  type: http
  seq: 7

http:
  method: GET
  url: "{{AUNTIE_SOM_NOODLE_BASE_URL}}/orders/23"
  auth:
    type: bearer
    token: "{{TOKEN}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"
    - expression: res.body.orderId
      operator: isUndefined
    - expression: res.body.error
      operator: isString

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
