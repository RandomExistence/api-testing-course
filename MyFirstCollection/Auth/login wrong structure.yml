info:
  name: login wrong structure
  type: http
  seq: 5

http:
  method: POST
  url: "{{AUNTIE_SOM_NOODLE_BASE_URL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "username":["Jump",1,1,"NOthing"],
        "password":{
          "1":1
        }
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "401"
    - expression: res.body.accessToken
      operator: isUndefined
    - expression: res.body.error
      operator: isString

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
